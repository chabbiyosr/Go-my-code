This function is made for correlation by using seaborn heatmap which is  a plot rectangular data as a color encoded matrix, first in this function we used  the.corr() method that get rid of the columns that are not swited for correlation.second, we create the size of a plot, and then a heatmap, we added the annotation which is a bool or can be a rectangular dataset and we choose the color map with cmap.p